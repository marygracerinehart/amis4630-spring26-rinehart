name: Backend CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore BuckeyeMarketplace/backend/BuckeyeMarketplaceAPI/BuckeyeMarketplaceAPI.csproj
    
    - name: Build backend
      run: dotnet build BuckeyeMarketplace/backend/BuckeyeMarketplaceAPI/BuckeyeMarketplaceAPI.csproj --configuration Release --no-restore
    
    - name: Run backend tests
      run: dotnet test BuckeyeMarketplace/backend/BuckeyeMarketplaceAPI.Tests/BuckeyeMarketplaceAPI.Tests.csproj --configuration Release --no-build --verbosity normal
      continue-on-error: true
    
    - name: Publish backend
      run: dotnet publish BuckeyeMarketplace/backend/BuckeyeMarketplaceAPI/BuckeyeMarketplaceAPI.csproj -c Release -o BuckeyeMarketplace/backend/publish

  deploy-to-azure:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '10.0.x'
    
    - name: Build for deployment
      run: dotnet publish BuckeyeMarketplace/backend/BuckeyeMarketplaceAPI/BuckeyeMarketplaceAPI.csproj -c Release -o BuckeyeMarketplace/backend/publish-release
    
    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'ndw38rn3iu'
        publish-profile: ${{ secrets.AZURE_BACKEND_PUBLISH_PROFILE }}
        package: BuckeyeMarketplace/backend/publish-release
