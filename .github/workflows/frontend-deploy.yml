name: Frontend CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'BuckeyeMarketplace/frontend/package-lock.json'
    
    - name: Install dependencies
      working-directory: ./BuckeyeMarketplace/frontend
      run: npm ci
    
    - name: Build frontend
      working-directory: ./BuckeyeMarketplace/frontend
      run: npm run build
      env:
        CI: true
        REACT_APP_API_URL: https://ndw38rn3iu-hfbzdmfhhkdmajev.centralus-01.azurewebsites.net
    
    - name: Run tests
      working-directory: ./BuckeyeMarketplace/frontend
      run: npm test -- --watchAll=false --passWithNoTests
      continue-on-error: true
    
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: frontend-build
        path: BuckeyeMarketplace/frontend/build

  deploy-to-azure:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend-build
        path: build
    
    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'buckeyemarketplace-frontend-05011243'
        publish-profile: ${{ secrets.AZURE_FRONTEND_PUBLISH_PROFILE }}
        package: build
